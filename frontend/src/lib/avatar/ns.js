/* One shared namespace, standing in for window.Nityam. The rig, emotion
 * engine, speech engine and mount all attach to it, exactly as they do in
 * the browser build of the sub-module. */
export const NS = {};
