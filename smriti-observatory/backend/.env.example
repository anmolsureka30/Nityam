GCP_PROJECT=nityam-506707
FIRESTORE_DATABASE=smriti
REDIS_HOST=localhost
REDIS_PORT=6379
# Base URL of whichever agent server exposes /memory/sessions/*: either
# sub_modules_examples/tutor's own uvicorn port (8010, per its README), or
# backend/'s (this repo's real production tutor -- port 8210, per its own
# run.sh/.env.example) -- both serve the same two GET routes.
TUTOR_BASE_URL=http://localhost:8000
