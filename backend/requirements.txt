google-adk==2.8.0
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-dotenv>=1.0.1
pydantic>=2.9
firebase-admin>=7.2

# Only needed when NITYAM_STORE=firestore. Installed either way so the switch is
# an environment variable rather than a reinstall.
google-cloud-firestore>=2.19
google-cloud-storage>=2.18
redis>=5.0

# Only needed when NITYAM_SHRUTI_MODE=cloud_run_job (app/shruti_routes.py) —
# triggering/polling the Shruti Cloud Run Job. Installed either way, same
# reasoning as the two lines above.
google-cloud-run>=0.16
